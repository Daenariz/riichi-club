[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "riichi-club"
version = "0.1.0"
dependencies = [
    "flask",
    "flask-wtf",
    "flask-sqlalchemy",
    "flask-migrate",
]

[tool.setuptools.packages.find]
include = ["app*"]
